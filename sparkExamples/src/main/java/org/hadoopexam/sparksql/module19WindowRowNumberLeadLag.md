Window 
	Window with partition by order is used to specify a window for ntile and lead lag function to work on data 

ntile 
	ntile is used for understanding to select data to find in how many section it can be divided

Lead and Lag 

We have to import org.apache.spark.sql.function._
	Lag - get the n th previous value of a column
	
	Lead - get the n th next value of a certain column
